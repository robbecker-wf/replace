name: replace
version: 1.1.0
description: An easy to use cross-platform regex replace command line util.
repository: https://github.com/robrbecker/replace

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.7.0
  cli_script: ^1.0.0
  file: ^7.0.1
  glob: ^2.1.3
  pubspec_manager: ^4.0.1
  yaml: ^3.1.3

dev_dependencies:
  path: ^1.9.1
  test: ^1.31.0

executables:
  replace:
  pm:
